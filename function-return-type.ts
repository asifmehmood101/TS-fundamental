//function return type and void

//it has number return type
function add(n1: number, n2: number) {
  return n1 + n2;
}

//it has void return type
function printValue(n) {
  console.log(n);
}

printValue(add(30, 40));
